# CI（Jenkins 用 npm install）依赖解析说明：
# monaco-editor@0.27 与 vite-plugin-monaco-editor@1.1 的 peer 版本不匹配
# （后者要求 monaco >=0.33）。本地用 pnpm（peer 宽松）一直正常构建，
# 这里让 npm 也接受既有解析，等价于 npm install --legacy-peer-deps。
legacy-peer-deps=true
